{
  "_comment_checkpoint_model": "Assumes full 30-epoch run completed (checkpoint_interval=10, so epoch 30 is the last saved). Confirm against the actual file on labgpu2 before running.",

  "mode": "eval",
  "seed": 1,

  "neural_dataset": "nsd",
  "region": ["early visual stream", "ventral visual stream"],
  "subject_idx": [0, 1, 2, 3, 4, 5, 6, 7],

  "analysis": "encoding_score",
  "compare_method": "spearman",
  "bootstrap": false,
  "n_bootstrap": 1000,
  "n_select": 1000,

  "batchsize": 256,
  "num_workers": 8,

  "load_model_from": "checkpoint",
  "checkpoint": {
    "checkpoint_dir": "model_checkpoints/ectiednet_imagenet50_depth8_baseline",
    "cfg_id": 1000,
    "checkpoint_model": "checkpoint_epoch_30.pth",
    "return_nodes": ["iter1", "iter2", "iter3", "iter4", "iter5", "iter6", "iter7", "iter8", "embedding"]
  },

  "log_expdata": true
}
